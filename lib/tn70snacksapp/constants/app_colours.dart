import 'package:flutter/material.dart';

class AppColors {
  // Primary & Branding
  static const Color primary = Color.fromRGBO(9, 51, 85, 1); // Main branding
  static const Color secondary = Color(0xFFEB5757); // Optional secondary red
  static const Color accent = Color(0xFFF2994A); // Optional accent (e.g., orange)

  // Backgrounds
  static const Color scaffoldBackground = Color(0xFFFFFFFF);
  static const Color imageBackground = Color(0xFFF2F2F2);
  static const Color cardBackground = Color(0xFFF8F9FA);

  // Buttons
  static const Color buttonColor = primary;
  static const Color buttonTextColor = Colors.white;

  // Text
  static const Color headingText = Color(0xFF333333);
  static const Color bodyText = Color(0xFF666666);
  static const Color labelText = Color(0xFF828282);
  static const Color iconColor = Color(0xFF4F4F4F);
  static const Color cartColor = Colors.grey;

  // Borders
  static const Color borderColor = Color(0xFFE0E0E0);

  // Status Colors
  static const Color success = Color(0xFF27AE60);
  static const Color error = Color(0xFFEB5757);
  static const Color warning = Color(0xFFF2C94C);
  static const Color info = Color(0xFF2F80ED);
}
